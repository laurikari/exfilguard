#!/bin/sh
set -e

if [ -d /run/systemd/system ]; then
    case "$1" in
        remove|upgrade|deconfigure)
            systemctl stop exfilguard.service >/dev/null 2>&1 || true
            ;;
    esac
fi

exit 0
